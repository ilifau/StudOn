<<<<<<<< HEAD:.github/workflows/install-check-release_8-8.0.yml
name: install-check-release_8-8.0
========
name: install-check-trunk-8.1
>>>>>>>> v9.1:.github/workflows/install-check-trunk-8.1.yml
on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:
jobs:
  installation:
    runs-on: ubuntu-20.04
    outputs:
      all: ${{ steps.changes.outputs.all }}
    strategy:
      fail-fast: false
      matrix:
<<<<<<<< HEAD:.github/workflows/install-check-release_8-8.0.yml
        php: [8.0]
========
        php: [8.1]
>>>>>>>> v9.1:.github/workflows/install-check-trunk-8.1.yml
    env:
      DB_DATABASE: ilias
      DB_USER: root
      DB_PASSWORD: root
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: release_8

      - name: Copy config file to location outside webroot
        run: |
           cp CI/install-check/ilias-ci-config.json ../ilias-ci-config.json

      - name: Copy config file to location outside webroot
        run: |
           cp CI/install-check/ilias-ci-config.json ../ilias-ci-config.json

      - name: Start MySQL Service
        run: |
           sudo /etc/init.d/mysql start

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: dom, curl, libxml, mbstring, zip, gd, json, readline, xsl, xml, mysql
          tools: composer:v2
          coverage: none

<<<<<<<< HEAD:.github/workflows/install-check-release_8-8.0.yml
      - name: Install dependencies
        run: composer install --no-interaction --no-progress --no-dev
========
      - name: Install Composer dependencies
        run: composer install --no-interaction --no-progress --no-dev

      - name: Install Node dependencies
        run: npm clean-install --omit-dev --ignore-scripts
>>>>>>>> v9.1:.github/workflows/install-check-trunk-8.1.yml

      - name: Perform setup
        run: |
           php setup/setup.php install -y ../ilias-ci-config.json
